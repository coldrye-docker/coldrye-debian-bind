# vim: noexpandtab:ts=4:sw=4

provider = coldrye
os = debian
release = jessie
feature = bind
feature_version = 9.9.5
tag := $(shell date +"%Y%m%d%H%M%S")
image = $(os)-$(feature)

include ../../Makefile.in

